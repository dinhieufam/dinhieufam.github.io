<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://dinhieufam.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://dinhieufam.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2026-06-21T15:01:14+00:00</updated><id>https://dinhieufam.github.io/feed.xml</id><title type="html">blank</title><subtitle>Personal academic website of Hieu Pham Dinh, a Data Science undergraduate at VinUniversity working on AI for robotics, multimodal learning, agentic AI, reinforcement learning, and real-world machine learning systems. </subtitle><entry><title type="html">Building Transformers from Scratch: Understanding the Architecture Behind Modern NLP</title><link href="https://dinhieufam.github.io/blog/2025/building-transformers-from-scratch/" rel="alternate" type="text/html" title="Building Transformers from Scratch: Understanding the Architecture Behind Modern NLP"/><published>2025-01-01T00:00:00+00:00</published><updated>2025-01-01T00:00:00+00:00</updated><id>https://dinhieufam.github.io/blog/2025/building-transformers-from-scratch</id><content type="html" xml:base="https://dinhieufam.github.io/blog/2025/building-transformers-from-scratch/"><![CDATA[<p>Transformers have revolutionized natural language processing and become the foundation of modern AI systems such as GPT, BERT, and countless other models. In this post, I will walk through the architecture from scratch, explaining each component and how they work together.</p> <h2 id="what-makes-transformers-special">What makes transformers special?</h2> <p>Unlike recurrent neural networks or convolutional neural networks, transformers rely entirely on <strong>self-attention mechanisms</strong> to process sequences. This allows them to:</p> <ul> <li>process all positions in parallel, making training much faster;</li> <li>capture long-range dependencies more effectively; and</li> <li>scale to very large models and datasets.</li> </ul> <h2 id="core-components">Core components</h2> <h3 id="1-self-attention">1. Self-attention</h3> <p>The heart of the transformer is self-attention. It allows each position in a sequence to attend to all other positions, computing a weighted sum of values based on query-key similarity.</p> <p>The attention formula is:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>Attention(Q, K, V) = softmax(QKᵀ / √dₖ) V
</code></pre></div></div> <p>Where:</p> <ul> <li><strong>Q (Query):</strong> What information am I looking for?</li> <li><strong>K (Key):</strong> What information do I have?</li> <li><strong>V (Value):</strong> What is the actual information content?</li> <li><strong>dₖ:</strong> What is the dimension of the key vectors used for scaling?</li> </ul> <h3 id="2-multi-head-attention">2. Multi-head attention</h3> <p>Instead of performing a single attention operation, transformers use multiple attention heads in parallel. Each head learns different aspects of the relationships between tokens, allowing the model to capture several kinds of dependencies at once.</p> <h3 id="3-positional-encoding">3. Positional encoding</h3> <p>Transformers do not have an inherent notion of sequence order, so positional information must be added to the input embeddings. The original paper used sinusoidal positional encodings:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>PE(pos, 2i)   = sin(pos / 10000^(2i/d_model))
PE(pos, 2i+1) = cos(pos / 10000^(2i/d_model))
</code></pre></div></div> <h3 id="4-feed-forward-networks">4. Feed-forward networks</h3> <p>Each transformer layer contains a feed-forward network that applies two linear transformations with a nonlinear activation in between:</p> <div class="language-text highlighter-rouge"><div class="highlight"><pre class="highlight"><code>FFN(x) = max(0, xW₁ + b₁)W₂ + b₂
</code></pre></div></div> <h3 id="5-layer-normalization-and-residual-connections">5. Layer normalization and residual connections</h3> <p>Transformers use layer normalization and residual connections around each attention and feed-forward sublayer. These choices help stabilize training, support deeper networks, and improve gradient flow.</p> <h2 id="architecture-overview">Architecture overview</h2> <p>The transformer architecture can be used in three broad configurations:</p> <ol> <li><strong>Encoder:</strong> Processes input sequences, as in BERT.</li> <li><strong>Decoder:</strong> Generates output sequences, as in GPT.</li> <li><strong>Encoder-decoder:</strong> Combines both, as in many machine translation systems.</li> </ol> <h2 id="key-implementation-details">Key implementation details</h2> <p>Several details are crucial when implementing transformers:</p> <ul> <li><strong>Masking:</strong> A decoder masks future positions to prevent information leakage during training.</li> <li><strong>Scaled dot-product attention:</strong> Dividing by \(\sqrt{d_k}\) prevents dot products from growing too large.</li> <li><strong>Stacking layers:</strong> Multiple transformer layers create progressively richer representations.</li> <li><strong>Normalization placement:</strong> Many modern implementations use pre-normalization instead of post-normalization.</li> </ul> <h2 id="why-transformers-work-so-well">Why transformers work so well</h2> <p>Transformers can model relationships between any two positions in a sequence, learn hierarchical representations through stacked layers, scale efficiently through parallel computation, and transfer knowledge effectively through pre-training.</p> <h2 id="conclusion">Conclusion</h2> <p>Understanding transformers from scratch provides valuable insight into how modern language models work. Self-attention, multi-head attention, positional information, and careful architectural choices create a framework that has transformed NLP.</p> <p>Whether you are working with BERT, GPT, or a custom architecture, grasping these fundamentals makes it easier to reason about model design and optimization. Future posts can take this foundation further into Vision Transformers, efficient attention mechanisms, and practical considerations for training large-scale models.</p>]]></content><author><name></name></author><category term="machine-learning"/><category term="transformers"/><category term="nlp"/><category term="deep-learning"/><category term="attention"/><summary type="html"><![CDATA[A ground-up guide to self-attention, multi-head attention, positional encoding, feed-forward networks, normalization, and residual connections.]]></summary></entry></feed>